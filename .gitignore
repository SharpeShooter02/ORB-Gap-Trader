# Credentials — never commit
.env

# Runtime data
data/*.db
data/*.db-wal
data/*.db-shm
data/logs/
data/archive/
data/underlyings/
data/reports/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Build / packaging
dist/
build/
*.egg-info/
.eggs/

# Testing
.pytest_cache/
.coverage
htmlcov/

# Virtual environment
.venv/
venv/
env/

# Editor
.vscode/
.idea/
*.swp
*.swo

# macOS
.DS_Store
.claude/worktrees/
# Live trading state. Paper-account trades, equity curve and P&L; it
# changes every session, so tracking it churned a ~600KB binary daily and
# put run artifacts in the code history. Existing history is untouched.
orb_live/state/*.db
orb_live/state/*.db-wal
orb_live/state/*.db-shm
